import {StyleSheet} from 'react-native';

const styles = props =>
  StyleSheet.create({
    containerStyle: {
      alignItems: 'center',
      justifyContent: 'center',
    },
  });
export default styles;
