import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  paginationContainer: {
    top: '100%',
  },
  pagination: {
    borderRadius: 3,
    borderWidth: 2,
  },
});

export default styles;
